/**
 * The five hero scenes. Also the five rotating headline verbs — the word and
 * the art change together (see `HeroSection`).
 */
export type HeroVariant = "design" | "build" | "architect" | "automate" | "ship";

export const HERO_VARIANTS: HeroVariant[] = [
  "design",
  "build",
  "architect",
  "automate",
  "ship",
];
